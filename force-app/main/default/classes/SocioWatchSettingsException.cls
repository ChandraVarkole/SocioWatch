/**
 * Safe exception for SocioWatch Settings operations.
 *
 * Messages can be surfaced to the UI. Never include credentials,
 * authorization headers, raw external responses, or stack traces.
 */
public class SocioWatchSettingsException extends Exception {
}
